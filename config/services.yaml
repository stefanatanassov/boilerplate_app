services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            $kernel.project_dir: '%kernel.project_dir%'

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
